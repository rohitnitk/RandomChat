const DEFAULT_NAME = "Anonymous";
const EMPTY_STRING = "";
const DAYS = 100;
const COOKIE_EXPIRY = DAYS * 24 * 60 * 60 * 1000;
const CONNECTED = "CONNECTED";
const WAIT = "WAIT";
const CHAT = "CHAT";
const NEXT = "NEXT";
const LEFT = "LEFT";
const INFO = "INFO";
const ERROR = "ERROR";

module.exports = {
  DEFAULT_NAME,
  EMPTY_STRING,
  COOKIE_EXPIRY,
  CONNECTED,
  WAIT,
  CHAT,
  LEFT,
  INFO,
  NEXT,
  ERROR,
};
