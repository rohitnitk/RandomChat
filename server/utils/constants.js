const DEFAULT_NAME = "Anonymous";
const EMPTY_STRING = "";
const UUID = "userId";
const DAYS = 100;
const COOKIE_EXPIRY = DAYS * 24 * 60 * 60 * 1000;

module.exports = { DEFAULT_NAME, EMPTY_STRING, UUID, COOKIE_EXPIRY };
